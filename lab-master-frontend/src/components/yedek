import React from 'react'
import { Form } from 'react-router-dom';
import {
    TextInput,
    PasswordInput,
    Checkbox,
    Anchor,
    Paper,
    Title,
    Text,
    Container,
    Group,
    Button,
} from '@mantine/core';
import classes from '../css/AuthenticationTitle.module.css';

function LoginForm() {
    return (
        <div>
            <Container size={420} my={40}>
                <Title ta="center" className={classes.title}>
                    Hoşgeldiniz!
                </Title>
                <Paper withBorder shadow="md" p={30} mt={30} radius="md">
                    <TextInput label="Email" placeholder="you@labmaster.dev" required />
                    <PasswordInput label="Şifre" placeholder="Şifre" required mt="md" />
                    <Group justify="space-between" mt="lg">
                        <Checkbox label="Beni Hatırla" />
                    </Group>
                    <Button fullWidth mt="xl">
                        Giriş Yap
                    </Button>
                </Paper>
            </Container>

        </div>
    )
}

export default LoginForm